# SPDX-License-Identifier: GPL-3.0-only

diff --git a/3d_accel.h b/3d_accel.h
--- a/3d_accel.h
+++ b/3d_accel.h
@@ -539,7 +539,11 @@
 #pragma pack(pop)
 
 /* DLL handlers */
+#ifdef GSW_DDHAL
+#define VMDISP9X_LIB "gswdd32.dll"
+#else
 #define VMDISP9X_LIB "vmdisp9x.dll"
+#endif
 
 typedef FBHDA_t *(__cdecl *FBHDA_setup_t)();
 typedef void (__cdecl *FBHDA_access_begin_t)(DWORD flags);
diff --git a/vmhal9x.c b/vmhal9x.c
--- a/vmhal9x.c
+++ b/vmhal9x.c
@@ -35,6 +35,10 @@
 
 #include "nocrt.h"
 
+#ifdef GSW_DDHAL
+#include "gsw_backend.h"
+#endif
+
 #ifndef DDHALINFO_ISPRIMARYDISPLAY
 #define DDHALINFO_ISPRIMARYDISPLAY 0x00000001l
 // indicates driver is primary display driver
@@ -335,6 +339,10 @@ DWORD __stdcall DriverInit(LPVOID ptr)
 	{
 		globalHal->d3dhal_global = 0;
 		globalHal->d3dhal_callbacks = 0;
+#ifdef GSW_DDHAL
+		/* mirrors the D3DHAL arm: dddrv.c ORs cb32.flags into ddHALInfo.dwFlags */
+		globalHal->cb32.flags = DDHALINFO_ISPRIMARYDISPLAY;
+#endif
 		memset(&globalHal->d3dhal_exebuffcallbacks, 0, sizeof(DDHAL_DDEXEBUFCALLBACKS));
 		memset(&globalHal->d3dhal_flags, 0, sizeof(VMDAHAL_D3DCAPS_t));
 	}
@@ -342,7 +350,11 @@ DWORD __stdcall DriverInit(LPVOID ptr)
 
 	globalHal->invalid = FALSE;
 	
-	if(FBHDA_load_ex(globalHal))
+	if(FBHDA_load_ex(globalHal)
+#ifdef GSW_DDHAL
+		&& GSWDD_load(globalHal)
+#endif
+	)
 	{
 		TRACE("DriverInit SUCCESS!");
 		return 1;
